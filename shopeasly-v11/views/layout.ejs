<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ShopEasly</title>
    <script>
        (function(){
            try {
                var saved = localStorage.getItem('theme') || 'light';
                document.documentElement.setAttribute('data-theme', saved);
            } catch (e) {
                document.documentElement.setAttribute('data-theme', 'light');
            }
        })();
    </script>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="icon" href="/images/shopeasly-logo.png">
            <!-- Chart.js (UMD) for global `Chart` variable; pinned for stability -->
            <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.4/dist/chart.umd.min.js"></script>
            <!-- Chart.js DataLabels plugin compatible with v4 -->
            <script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2"></script>
            <script>
                // Safely register DataLabels if present; avoids runtime errors when not used
                if (window.Chart && window.ChartDataLabels && typeof Chart.register === 'function') {
                    Chart.register(ChartDataLabels);
                }
            </script>
    <script src="https://cdn.jsdelivr.net/npm/marked@9.1.6/marked.min.js"></script>
    <meta http-equiv="Content-Security-Policy" content="upgrade-insecure-requests">
    <style>
        /* Ensure basic fonts render consistently if custom fonts fail */
        body { font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif; }
    </style>
    <!-- Prevent search indexing -->
    <meta name="robots" content="noindex,nofollow">
    <meta name="referrer" content="no-referrer">
</head>
<body>
    <div class="app-container">
        <aside class="sidebar">
            <div class="sidebar-header">
                <div class="logo">
                    <img src="/images/shopeasly-logo.png" alt="ShopEasly" style="height:28px; width:auto; margin-right:8px;">
                    <span>ShopEasly</span>
                </div>
                <div class="sidebar-profile">
                    <div class="avatar" style="width: 32px; height: 32px; background: var(--primary); display:flex; align-items:center; justify-content:center; color: white; font-size: 0.8rem;">AI</div>
                    <div>
                        <div style="font-weight:600; font-size:0.9rem;">AI-Powered</div>
                        <div style="font-size:0.8rem; color: var(--text-secondary);">Inventory System</div>
                    </div>
                </div>
            </div>

            <nav class="sidebar-nav" aria-label="Primary">
                <ul>
                    <li><a class="nav-btn" href="/">Dashboard</a></li>
                    <li><a class="nav-btn" href="/orders">Orders</a></li>
                    <li><a class="nav-btn" href="/inventory">Inventory</a></li>
                    <li><a class="nav-btn" href="/easly">AI Assistant</a></li>
                </ul>
            </nav>

            <div class="sidebar-footer">
                <div class="theme-picker">
                    <button class="theme-btn" data-theme="light" onclick="setTheme('light')" title="Light Theme">Light</button>
                    <button class="theme-btn" data-theme="dark" onclick="setTheme('dark')" title="Dark Theme">Dark</button>
                </div>
            </div>
        </aside>

        <div id="main-content">
            <header class="main-header">
                <div style="display:flex; align-items:center; gap:1.2rem;">
                    <a href="/" class="btn btn-secondary" style="font-size:1.1rem; padding:8px 18px; text-decoration:none; display:flex; align-items:center; gap:0.5rem;">Home</a>
                    <button id="sidebar-toggle-btn" class="btn btn-secondary" aria-label="Toggle sidebar" title="Toggle sidebar" onclick="toggleSidebar()">Menu</button>
                    <div>
                        <h1 style="margin:0; color:var(--text-primary);">ShopEasly</h1>
                        <p style="margin:4px 0 0 0; color:var(--text-secondary); font-size:0.9em;">Operations & order management</p>
                    </div>
                </div>
                <div class="header-actions">
                    <input type="search" class="search-input form-input" placeholder="Search...">
                    <button class="btn-icon" title="Notifications">Notifications</button>
                    <button class="btn-icon" title="Settings">Settings</button>
                </div>
            </header>

            <div id="app-views">
                <%- body %>
            </div>
        </div>
    </div>

    <button class="ai-fab" onclick="window.location.href='/easly'" title="AI Assistant">AI</button>

    <script>
        function setTheme(theme) {
            document.documentElement.setAttribute('data-theme', theme);
            localStorage.setItem('theme', theme);
            document.querySelectorAll('.theme-btn').forEach(btn => btn.classList.remove('active'));
            var t = document.querySelector('.theme-btn[data-theme="' + theme + '"]');
            if (t) t.classList.add('active');
        }

        const savedTheme = localStorage.getItem('theme') || 'light';
        document.documentElement.setAttribute('data-theme', savedTheme);

        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.theme-btn').forEach(btn => {
                if (btn.dataset.theme === savedTheme) {
                    btn.classList.add('active');
                }
            });
        });

        (function(){
            const currentPath = window.location.pathname.replace(/\/$/, '');
            document.querySelectorAll('.sidebar-nav a.nav-btn').forEach(link => {
                const href = (link.getAttribute('href') || '').replace(/\/$/, '');
                const isActive = href === currentPath || (href && href !== '/' && currentPath.startsWith(href + '/'));
                if (isActive) {
                    link.classList.add('active');
                    link.setAttribute('aria-current', 'page');
                }
            });
        })();

        function toggleSidebar() {
            const app = document.querySelector('.app-container');
            app.classList.toggle('sidebar-open');
            document.body.classList.toggle('no-scroll', app.classList.contains('sidebar-open'));
        }
    </script>

    <div class="loading-overlay" id="global-loading-overlay" style="display:none;">
        <div class="loading-content">
            <div class="spinner-large"></div>
            <p>Loading...</p>
        </div>
    </div>
    <div id="toast-notification"></div>
    <script>
        if (typeof window.showToast !== 'function') {
            window.showToast = function(message, type = 'info') {
                let toast = document.getElementById('toast-notification');
                if (!toast) {
                    toast = document.createElement('div');
                    toast.id = 'toast-notification';
                    document.body.appendChild(toast);
                }
                toast.textContent = message;
                toast.className = `show ${type}`;
                setTimeout(() => { toast.classList.remove('show'); }, 3000);
            }
        }
        if (typeof window.showLoading !== 'function') {
            window.showLoading = function() {
                const el = document.getElementById('global-loading-overlay');
                if (el) el.style.display = 'flex';
            }
        }
        if (typeof window.hideLoading !== 'function') {
            window.hideLoading = function() {
                const el = document.getElementById('global-loading-overlay');
                if (el) el.style.display = 'none';
            }
        }
    </script>

    <script src="/js/copilotModal.js"></script>
    <script src="/js/voicePromptModal.js"></script>
</body>
</html>

